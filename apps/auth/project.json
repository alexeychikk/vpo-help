{
  "root": "apps/auth",
  "sourceRoot": "apps/auth/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/auth",
        "main": "apps/auth/src/main.ts",
        "tsConfig": "apps/auth/tsconfig.app.json",
        "webpackConfig": "apps/auth/webpack.config.js",
        "generatePackageJson": true
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:execute",
      "options": {
        "buildTarget": "auth:build",
        "inspect": "inspect",
        "host": "0.0.0.0",
        "port": 9227
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/auth/**/*.ts"]
      }
    },
    "ts": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          "tsc -p apps/auth/tsconfig.app.json --noEmit",
          "tsc -p apps/auth/tsconfig.spec.json --noEmit"
        ]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/auth"],
      "options": {
        "jestConfig": "apps/auth/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
